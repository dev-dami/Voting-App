# LAN override for the API target (REST + /api/live/stream websocket).
# Dev: vite proxies /api (including upgrades) to this target.
# Build: the client calls this origin directly (same-origin when empty).
# Example LAN use: VITE_API_URL=http://192.168.1.10:3000
VITE_API_URL=http://localhost:3000
